let interpreter ((input : string), (output : string )) : unit = ...

Read a program (input file) and executes, then, prints output into output file.

- input : read string data (lines of commands) and sort them into a list
